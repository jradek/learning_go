{
    "version": "0.2.0",
    "configurations": [

        {
            "name": "Hello",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}/hello/src/hello/main.go",
            "env": {
                "GOPATH": "${workspaceFolder}/hello"
            }
        }
    ]
}